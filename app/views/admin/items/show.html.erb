<h4>商品詳細</h4>
<%= image_tag @item.get_image, size: "200x100" %>

<div>
  <div>商品名</div>
  <div>
    <%= @item.name %>
  </div>
  <div>商品説明</div>
  <div>
    <%= @item.introduction %>
  </div>
  <div>税込み価格<br>
  (税抜き価格)</div>
  <div>
    <%= @item.price %>(<%= (@item.price / 1.1).round %>)円
  </div>
  <div>
    <%= link_to edit_admin_item_path(@item) do %>
      編集する
    <% end %>
  </div>
</div>